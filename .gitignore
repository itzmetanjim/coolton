# Build
build/
dist/
docs/_sources/
docs/.doctrees

# Claude Code
.claude/*.local.json

# Coverage
.coverage
cov_*
coverage.xml

# Editors
.idea/

# Environment
.env*
!.env.sample
env*/
venv/
.venv*

# Logs
logs/

# macOS
.DS_Store

# OAuth
data/
oauth_errors.log

# Skills CLI install cache (npx skills add drops here; curated skills live in skills/)
# Ignore the .agents dir by default but keep selected skill directories tracked.
.agents/*
!.agents/skills/
# These CLI skills were merged into curated skills/ — do not re-track them.
.agents/skills/find-skills/
.agents/skills/fusion-skill-authoring/
.agents/skills/no-ai-slop/

# Python
__pycache__/
*.db
*.egg
*.egg-info/
*.eggs/
*.py[cod]
*.so
.mypy_cache/
.pytype/
.python-version
*~
pip

# Testing
.cache
.pytest_cache/
.tox
tmp.txt
manifest.json

# Runtime state (regenerated at runtime; contains private Slack IDs)
conversations.json
fallback_cache.json
mcp_health.json
leave_thread_store.json
ban_store.json
reminders.json
scheduled_tasks.json
thread_sandboxes.json
byok_key.bin
mcp_server_store.json
web_conversations/
web_attachments/
mcp_server_key.bin

# Local Slack CLI project metadata (team IDs, local paths)
.slack/

# Stale scratch file
aaa.py

# Skill CLI install lock (machine-generated; paths drift from actual install location)
skills-lock.json

# Keep skills tracked: .agents/skills/ is whitelisted above; curated skills/ is tracked.

# Redacted, complete Slack thread traces
conversation_logs/

# Local policy consent and pending requests
policy_consents.json

# Slack CLI (xoxe) token pair, rotated automatically by agent/token_rotation.py
xoxe_tokens.json
xoxe_tokens.json.tmp

# Secure Slack bot credential store (created by agent/tools/slack_bot_deploy.py)
coolton_bots.json
~/.coolton_bots.json

# Local-dev web64 helper: token + uploaded files (prod uses /home/tanjim/web64_*)
.local/
