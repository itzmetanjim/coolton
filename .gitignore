# Build
build/
dist/
docs/_sources/
docs/.doctrees

# Claude Code
.claude/*.local.json

# Coverage
.coverage
cov_*
coverage.xml

# Editors
.idea/

# Environment
.env*
!.env.sample
env*/
venv/
.venv*

# Logs
logs/

# macOS
.DS_Store

# OAuth
data/

# Skills CLI install cache (npx skills add drops here; curated skills live in skills/)
# Ignore the .agents dir by default but keep selected skill directories tracked.
.agents/*
!.agents/skills/
# These CLI skills were merged into curated skills/ — do not re-track them.
.agents/skills/find-skills/
.agents/skills/fusion-skill-authoring/
.agents/skills/no-ai-slop/

# Python
__pycache__/
*.db
*.egg
*.egg-info/
*.eggs/
*.py[cod]
*.so
.mypy_cache/
.pytype/
.python-version
*~
pip

# Testing
.cache
.pytest_cache/
.tox
tmp.txt
manifest.json

# Runtime state (regenerated at runtime; contains private Slack IDs)
conversations.json
fallback_cache.json
leave_thread_store.json
reminders.json
thread_sandboxes.json
byok_key.bin

# Local Slack CLI project metadata (team IDs, local paths)
.slack/

# Stale scratch file
aaa.py

# Skill CLI install lock (machine-generated; paths drift from actual install location)
skills-lock.json

# Keep skills tracked: .agents/skills/ is whitelisted above; curated skills/ is tracked.
